name: termino
description: "A cross-platform terminal emulator and SSH client."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  collection: ^1.19.1
  cupertino_icons: ^1.0.8
  dartssh2: ^4.1.0
  drift: ^2.34.4
  drift_flutter: ^0.3.1
  file_picker: ^12.2.0
  flutter:
    sdk: flutter
  flutter_pty: ^0.4.2
  flutter_riverpod: ^3.4.3
  flutter_secure_storage: ^11.0.0
  freezed_annotation: ^3.1.0
  go_router: ^18.0.1
  json_annotation: ^4.12.0
  local_auth: ^3.0.2
  logging: ^1.3.0
  meta: ^1.19.0
  path: ^1.9.1
  path_provider: ^2.1.6
  pinenacl: ^0.6.0
  pointycastle: ^4.0.0
  riverpod_annotation: ^4.0.7
  share_plus: ^13.3.0
  url_launcher: ^6.3.2
  web_socket_channel: ^3.0.3
  window_manager: ^0.5.2
  xterm: ^4.0.0

dev_dependencies:
  build_runner: ^2.16.1
  drift_dev: ^2.34.6
  fake_async: ^1.3.3
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.8
  flutter_test:
    sdk: flutter
  freezed: ^4.0.1
  integration_test:
    sdk: flutter
  json_serializable: ^6.14.1
  msix: ^3.18.0
  riverpod_generator: ^4.0.9
  riverpod_lint: ^3.1.9
  # The reference relay, so tests can run a real one in-process and prove the
  # web path end to end rather than assuming it.
  termino_relay:
    path: tools/relay
  very_good_analysis: ^11.0.0
flutter:
  uses-material-design: true

  fonts:
    # JetBrains Mono, patched by Nerd Fonts. Bundled rather than taken from the
    # system because terminal output is full of box drawing, block elements and
    # Powerline separators, and a system monospace that lacks them renders tofu
    # in the middle of htop. Both the typeface and the patches are OFL-1.1; the
    # licence ships alongside them in assets/fonts/.
    - family: JetBrainsMono
      fonts:
        - asset: assets/fonts/JetBrainsMonoNerdFontMono-Regular.ttf
        - asset: assets/fonts/JetBrainsMonoNerdFontMono-Bold.ttf
          weight: 700
        - asset: assets/fonts/JetBrainsMonoNerdFontMono-Italic.ttf
          style: italic
        - asset: assets/fonts/JetBrainsMonoNerdFontMono-BoldItalic.ttf
          weight: 700
          style: italic

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Windows installer. Unsigned unless a certificate is configured, which means
# SmartScreen warns on first run — see docs/RELEASING.md.
msix_config:
  display_name: Termino
  publisher_display_name: Termino contributors
  identity_name: com.termino.app
  logo_path: assets/icon/icon.png
  capabilities: internetClient, privateNetworkClientServer
  install_certificate: false
